# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage
osx_image: xcode9.2
language: objective-c
# cache: cocoapods
podfile: Example/Podfile
notifications:
  email: false
before_install:
 - gem install bundler
 - bundle install
 - bundle update fastlane
 - export GITHUB_TOKEN=$GITHUB_TOKEN
script:
 - bundle exec fastlane test
after_success:
 - bash <(curl -s https://codecov.io/bash)
 - bundle exec fastlane deploy


# xcodebuild test -enableCodeCoverage YES -workspace Example/TCJSON.xcworkspace -scheme TCJSON-Example -sdk iphonesimulator9.3 ONLY_ACTIVE_ARCH=NO | xcpretty
# xcodebuild test -destination "platform=iOS Simulator,OS=11.2,name=iPhone 7" -workspace TCJSON.xcworkspace -scheme TCJSON-Example -enableCodeCoverage YES ONLY_ACTIVE_ARCH=NO | xcpretty
